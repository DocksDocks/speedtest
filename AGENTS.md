# AGENTS.md

## Project Map

- Purpose: local network diagnostics, raw-log ingestion, SQLite metric storage, and static browser reporting.
- Shell entry points: `scripts/*.sh`.
- Shared shell libraries: `scripts/lib/path.sh`, `scripts/lib/sqlite.sh`, `scripts/lib/classification.sh`, `scripts/lib/report.sh`, `scripts/lib/networkmanager.sh`, `scripts/lib/colors.sh`, `scripts/lib/warnings.sh`.
- SQLite schema: `db/schema/*.sql`; seed/default data: `db/defaults/*.sql`, `db/seed.sql`.
- Browser report: `index.html`, `assets/report.css`, `assets/report.js`.
- Project-local skills: `.agents/skills/sqlite3-quality/SKILL.md`, `.agents/skills/bash-diagnostics/SKILL.md`.
- Generated/private data: `logs/*`, `logs/*.sqlite*`, `data/*`, `reports/*.html`.

## Architecture Rules

- `index.html`, `assets/report.css`, and `assets/report.js` are committed static assets.
- `data/report.sqlite` is generated by `scripts/publish-report-db.sh` and must stay ignored.
- Browser rendering queries `data/report.sqlite` through SQLite WebAssembly; do not generate machine-specific HTML.
- The working database is `logs/speedtest.sqlite`; publish a browser snapshot with `VACUUM INTO` instead of serving an active WAL database.
- Raw command output belongs in `raw_logs`; classification metadata belongs in `raw_log_classifications`.
- New parsed measurements should use a dedicated table in `db/schema/` plus report queries in `assets/report.js`.

## SQLite Rules

- Use `.agents/skills/sqlite3-quality/SKILL.md` before changing schema, queries, sqlite3 CLI usage, report publishing, or browser SQLite rendering.
- Use `scripts/lib/sqlite.sh` helpers for shell writes so `PRAGMA foreign_keys = ON` and a 5000 ms CLI timeout are applied per connection.
- Keep WAL for the local working DB; convert the browser snapshot to rollback-journal mode in `scripts/publish-report-db.sh`.
- Run schema changes through `scripts/init-db.sh`; do not mutate `logs/speedtest.sqlite` manually in docs or scripts.
- Prefer explicit tables/views over storing parsed metrics only in raw log text.

## Shell Rules

- Use `.agents/skills/bash-diagnostics/SKILL.md` before changing collection, ingestion, report, rollback, NetworkManager, or iwd scripts.
- Source `scripts/lib/path.sh` or `scripts/lib/sqlite.sh` for project paths; do not hard-code absolute user paths.
- Source `scripts/lib/warnings.sh` when a script has required commands so missing dependency output stays aggregated and includes install hints.
- Detect Wi-Fi interface/profile with `speedtest_default_wifi_iface` and `speedtest_default_connection_name`.
- Site-specific values such as SSID, BSSID, DNS search domain, or local gateway must be env vars or DB data, not committed defaults.
- Scripts that change NetworkManager, DNS, Avahi, or iwd must require explicit confirmation or documented root invocation.

## Privacy Rules

- Never commit generated logs, SQLite DBs, generated report snapshots, package caches, hostnames, SSIDs, BSSIDs, local IPs, or DNS search domains.
- Before publishing, grep committed sources for machine-specific values and inspect `git status --short --ignored`.
- `.gitignore` intentionally ignores `logs/*`, `logs/*.sqlite*`, `data/*`, and `reports/*.html`; keep `.gitkeep` exceptions.

## Verification

- Shell syntax: `bash -n scripts/*.sh scripts/lib/*.sh`.
- Browser JS syntax: `node --check assets/report.js`.
- DB initialization: `scripts/init-db.sh`.
- Browser snapshot: `scripts/publish-report-db.sh`.
- Static report server: `scripts/serve-report.sh` and open `http://127.0.0.1:8765/`.

<constraint>
Research current documentation before adding or changing external tools, SQLite browser dependencies, NetworkManager settings, or package installation guidance.
</constraint>

<constraint>
Do not replace the static report with generated HTML; keep variable report data in ignored SQLite snapshots.
</constraint>
